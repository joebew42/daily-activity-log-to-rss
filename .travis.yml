language: python
python:
  - "2.7"
install:
  - "pip install -r requirements.txt"
  - "pip install coveralls"
script:
  - coverage run --source=src manage.py test
  - coverage run --source=src manage.py integration-test
after_success:
  coveralls